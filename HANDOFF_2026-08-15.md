# Handoff — 2026-08-15, Predator (InspectorX)

Branch **`inspx/road-network`** @ `48d2a0f7`, pushed, **97 commits ahead of `origin/main`**.
All suites green: rooms 16/16 · fitout 14/14 · mouth 7/7 · road 6/6 · corridor 9/9 ·
precip 8/8 · storm 11/11 · wetness 30/30 · weather 23/23 · captures exit 0.
(`--test-vehparts` 22/1 is the KNOWN pre-existing street-vs-stock wheelspin, task #25.)

---

## THE ONE THING TO CARRY FORWARD

**Eight times today a "missing" feature turned out to be already built behind a
different name.** EchoRoads · the wetness model · the weather system · the editor's
"Details" panel (I grepped "Properties|Inspector" and missed it) · the AI architect ·
the on-foot Player controller · the terrain's altitude snow band · and **Tim's own
shop, fully authored in `app/club1127.cpp`** — which I only found after building a
generic one for an hour.

That is not coincidence, it is the shape of this codebase. **Prove it is not already
there before building it.** Grep for the *concept* and its synonyms, not just your
chosen name.

---

## LIVE NOW (this branch)

* **Weather**: temperature is the spine (`WeatherSample::tempC`) — it was the missing
  link that let weather drive wetness. Snow accumulates with degree-hour melt;
  meltwater wets the road. Lightning + thunder with real travel delay (0.7 s near →
  26.2 s far) and rolling echo tails. Thermometer HUD. **Console-driven**: backtick,
  then `wx off|clear|cloudy|rain|storm|fog|snow`, `wx_snow_in`, `wx_hour`.
* **Snow blows INTO the portal** — graded by distance to the opening via an upward
  ray that knows nothing about tunnels (works under a bridge too).
* **Tunnel**: bore widened 39 ft → **48 ft running / 54 ft interior** because the ring
  road is 4×12 ft and *the bore was narrower than the road feeding it*. Walkways,
  railings, subway tile band, signage, screens, door recesses. 11 spaces behind 4
  doors + a **garage** (100×43×24 ft, down an 87 ft ramp, 323 ft of rock over it) with
  Tim's real LNSS kit: 3 Rotary two-post, Hunter alignment rack, Road Force balancer,
  tire machine, Miller, Robinair. 6 cars parked in it.
* **E gets you out of the car** (proximity-gated re-entry, parked car handbraked),
  **Jake in third person** over the shoulder.
* **11 vehicles** converted in `assets/converted_glb/Vehicles/`; driving `E46_New`,
  the only one with a cabin.
* **Editor**: inspector gained name/type/proportional-scale slider; **model picker**;
  Qwen2.5-**7B** (Apache-2.0) pulled and verified running (9–12 tok/s, CPU).

## IN FLIGHT — two Fable agents, own worktrees, do not duplicate

| branch | job |
|---|---|
| `inspx/outer-ring` | ~31-mile outer ring boring through the 4 ranges + make the river actually get crossed |
| `inspx/lnss-garage` | dress the tunnel bay with the AUTHORED Club 1127 shop from `club1127.cpp` |

## NEXT

1. Collect/review/merge those two branches.
2. **LA-EXE** (`inspx/la-exe`, 33 ahead) — waiting on the 14900k push Tim ordered.
   Build the merge result **Release AND Debug** (a DLL split is exactly the change
   that compiles clean on its branch and breaks the tip).
3. Unbuilt and asked for: garage screen to choose a car; **secret door → sweeping
   bore inside the mountain → summit base**.
4. Reconcile the freeze claim: `ceb3c484` is already an ancestor of `origin/main`,
   which has moved to `d3a17834`.

## TRAPS THAT COST REAL TIME TODAY

* **Captures write relative to CWD** — `<builddir>/bin/docs/screenshots/`, NOT the
  repo root. I read a stale repo-root PNG for an hour and concluded a working
  feature was broken.
* **Headless has no window.** `glfwSetCharCallback(nullptr,…)` is an access
  violation; it killed every capture and I bisected onto an innocent car model.
  Interactive-only testing hides it, and no test opens a window.
* **A suite can be vacuously true.** Lay-bys silently produced ZERO while every
  assertion passed, because they only checked survivors were legal — never that
  anything survived. Assert non-empty.
* **A python `.replace()` that does not match silently no-ops.** Assert after every
  scripted edit, then grep.
* **Bash `cmd //c` builds silently do nothing here.** Use the PowerShell tool with
  `vcvars64.bat`. An identical test number twice in a row means it did not rebuild.
* **Units: Tim reads FEET and MILES.** Convert at the boundary, never in the middle.
