# ⚠️ SYNC FIRST — you are 49 commits behind `origin/main` (READ THIS BEFORE ANY MORE WORK)

Your branches (`feat/doors-death-anim`, `feat/multi-font-roles`, `backup/visual-pass-good`) are **~49 commits behind `origin/main`** and hold great work that is **NOT on main yet** (fonts / multi-font HUD + TTF, **exploding barrels**, **map update**, **resolution change**, sci-fi textures, doors + death-anim, Consolas console font, animated drone). Meanwhile your base is missing the **49 commits** the rest of the fleet landed: the real **283 m Spire re-lay** (`level1.cpp` was heavily rewritten), the **strata/disco elevator + Club 1127**, **Act-2** L8–15 + the **open world** (world_regions/city/ocean_base), **dialog + timeline/karma**, the blueprint + the `x3native-environments` skill. **Reconcile now — the gap grows every commit.**

## DO THIS (catch up, then the integrator merges your work in)
1. Pick your most-complete branch (likely `feat/doors-death-anim`). `git fetch origin`.
2. **`git merge origin/main`** (one reconciliation pass — easier than rebasing 39 commits).
3. **Resolve conflicts WITH your context.** Expected hot spots:
   - **`app/level1.cpp`** — the biggest: the Spire was re-laid to the real **283 m non-uniform** stack (floors at Y 0/10/20/30/65/78/91/104, alien tunnels, caves, sub-levels). KEEP that new structure AND fold in YOUR map/texture changes on top — don't revert the re-scale.
   - **`app/main.cpp`** + **`app/CMakeLists.txt`** — union of `--test-*` flags + source lists from all lanes; keep BOTH sides' additions (yours: fonts/barrels/resolution; theirs: elevator/club/act2/world/dialog/timeline). Resolution change: re-apply yours on top of the current window/swapchain code.
   - **`app/ui.cpp` / `app/hud.cpp`** — your font system vs any fleet HUD edits; keep your fonts, merge their changes.
   - **`app/env_art.*` / textures** — your sci-fi textures + the converted-GLB pipeline.
4. Build + gate: `windows-vs2026` preset + the FULL `--test-*` set (your new flags + everyone's) + Release/Debug `--smoketest` **0 VUID + `allocationCount=0`**.
5. `git push origin feat/doors-death-anim` (your merged branch). **Do NOT push `main`** — tell the 13700K integrator; it merges your caught-up branch → main (clean) so your fonts/barrels/map/resolution/textures land and you're fully current.

*(Doing it the other way — the integrator merging your 49-behind branch — hits the same conflicts WITHOUT your context, so YOUR-side merge first is the safe path.)*

---

# TASK FOR 14900K + RTX 5090 — assigned by 13700K integrator

**You are a clean-room TOOLS engineer on X3Native** (native C++20/Vulkan 1.3, this repo). Work in YOUR clone, push a feature branch, report status at the bottom. The 13700K integrator merges + re-gates + pushes `main` — **you do NOT push `main`.** You're idle with the 5090 — this is a GPU-viewport tool lane, a perfect fit.

## ABSOLUTE CLEAN-ROOM RULE
NEVER read/reference/copy from RBDOOM, id Tech, Doom, Quake, or ANY other game-ENGINE source. You MAY read Tim's OWN tools (the Babylon **LevelArchitect v10.9** + **Task9D** 3D editor) for UX reference + the EFLZ design docs. Build the native tool from X3Native's own interfaces only.

## YOUR TASK: a native LEVEL / WORLD EDITOR
Today the Spire + Act-2 worlds are hand-coded in C++ (`level1.cpp` `addBox`, `act2_world.*`/`act2_desert.*`/`act2_caves.*`). Build a native EDITOR so levels/worlds are AUTHORED visually + saved to a data file the engine LOADS — replacing hardcoded geometry over time. Model the UX on Tim's own `LevelArchitectFullV10.9` + `Task9D_AllFloors` editors (you can SEE the target there).

**Build it as a SEPARATE concern** — a new `tools/editor/` tree (or `app/editor*.{h,cpp}` behind a `--editor` mode) + minimal `app/main.cpp` / `app/CMakeLists.txt` wiring. Do NOT edit the game level modules (`level1.*`, `act2_*`, `spire_*`, `monster.*`) — read their data structs only. (Collision points are just `main.cpp`/`CMakeLists` unions, which the integrator resolves.)

READ FIRST:
- `docs/design/X3_WORLD_BLUEPRINT.md` (target world + real dims) + `docs/design/SPIRE_LEVELARCHITECT_DIMS.md`.
- `G:\GameDev\LevelArchitectFullV10.9\js\` (App / UI / Gizmo / Selection / State.js) + `…\OneDrive\GameDev\DellGameDev\Escape48BLN\Task9D_AllFloors_v10_3D_Models & Editor.html` — Tim's OWN editor UX to MIRROR (room-type palette, layer toggles, fly/orbit/FPS cams, the floor list, export JSON).
- `app/level1.h` (`L1RoomDef`, `L1DetentionRoom`, `Level1Layout`) + `app/act2_world.h` (`Act2AreaPlan`) — the data shapes the editor reads/writes.
- `engine/rhi/IRenderDevice.h` (mesh / line / camera APIs for the viewport).

IMPLEMENT (incrementally; each step gateable):
1. **Viewport + cameras** — Orbit (drag), Fly (WASD), FPS-walk; grid + axis gizmo; render an existing level (`buildLevel1()` or an Act-2 world) so it's visible.
2. **Selection + transform gizmos** — click-pick a room/box/entity; move/rotate/scale handles (mirror LevelArchitect `Gizmo.js`).
3. **Place / delete** — add rooms by type from a palette (like `LA.DEFS`/`LA.TC`), props, entities, triggers; per-floor / per-region **layer toggles** (like the Task9D legend checkboxes).
4. **Save / Load a level-data format** — a versioned file (JSON or binary `.x3lvl`) capturing rooms/props/entities/triggers + a tiny LOADER the engine can call to build a level FROM the file. Proof: round-trip a hand-authored room → save → load → render identical. **This is the payoff — data-driven levels.**
5. *(stretch)* import Tim's LevelArchitect `FLOOR1_DEFAULT` JSON → editor → native level data.

TEST: a headless `--test-editor` self-test (round-trip a level-data doc in memory → serialize → deserialize → assert identical rooms/props/triggers; the loader produces the expected scene entity count). Keep every other `--test-*` green.

## WORKFLOW (in your clone)
1. `git fetch origin && git checkout -b feat/level-editor origin/main`.
2. Implement. Commit frequently; commit a working state BEFORE the final build.
3. Build + gate (PowerShell): the standard `windows-vs2026` preset + the full `--test-*` set + `--test-editor`; Release `--smoketest` 0 VUID; Debug `--smoketest` 0 VUID AND `allocationCount=0`.
4. `git push origin feat/level-editor`. Do NOT touch `main`.

## REPORT STATUS (append below, then push the branch)
<!-- STATUS: branch HEAD, files added, --test-editor result, all-flags-0 + VUID 0 + allocationCount=0, "READY FOR INTEGRATION" or BLOCKED+why. -->

### STATUS — 2026-05-25
**The Level-Editor lane is NOT STARTED.** Tim (owner) redirected this machine to a live gameplay/visual-polish playtest + the big main↔session integration instead. So this lane is **DEFERRED**, not blocked — pick it up next session.

**What this machine DID do (full report + integration handoff in `docs/NOTE_TO_13700K.md`, 2026-05-25 entry):** a large gameplay/visual pass on `feat/doors-death-anim` (role fonts, RT-AO, gibs, NPC dialog, real weapon models+sounds, flashlight, holo terminal, minimap, doors, textures, noclip) **+ executed your SYNC-FIRST directive**: `integration/main-plus-session` (`fb7c26e`) merges `origin/main` + this session, **builds + 37 tests green**, backups tagged, now folded into `feat` — **READY FOR YOU TO PROMOTE to `main`**. ⚠️ I crossed into `engine/rhi` (TTF font atlas + gated `r_rtao` RT-AO) — please review.

### STATUS — 2026-06-08 (14900K / RTX 5090) — checking in on the board 👋
**MY ASSIGNED EDITOR LANE: DONE** (built it this session — only just realized it was *formally* my task). Native Level Architect editor shipped on `feat/doors-death-anim`:
- Viewport + fly-cam + grid, F8 Edit/Play, live `--editor`.
- Box/Ramp brushes, snap-to-grid, Jolt collision bridge.
- **Move/Rotate/Scale gizmos** + **ray-vs-OBB pick**; **undo/redo** command stack.
- **Click-a-wall texturing** (Materials palette, per-brush) + **model browser** (place GLB props).
- **Doom-Builder keyboard nudge editing** (look-at-brush → move/stretch/floor-ceiling height, grid-snapped, undoable, **rebindable** keymap + unobtrusive cheat-sheet).
- **JSON save/load** (LevelDoc round-trips). Gates: `--test-editor 19/19`, `--test-blockout 5/5`, `--smoketest` 0 VUID / allocationCount=0.
- ⏭️ Remaining editor increment per the task: the engine-side **loader that BUILDS a level from the saved data** (replace hardcoded geometry) — the editor JSON round-trips; the load-into-engine step is next.

**ALSO shipped this session (bonus, same branch):**
- **Showroom HDRP material fix** (MaskMap→ORM, `_BaseColor` tints, localized emissive) — root-caused a baked **wall-emissive** white-out; + **interior reflection probe**, **IBL specular energy-limit**, **`r_exposure`** dial.
- **DetailMaps** end-to-end (converter → loader → device → shader).
- **Glass-bottom centre elevator**; **Quake3 lightning gun** (jagged white-blue bolt + Vefects zap loop); **weapon-grip live-tune tool** (`grip_*` cvars — Tim dials, I bake).

**⚠️ SYNC STILL OWED:** `feat/doors-death-anim` is **48 ahead / 43 behind `origin/main`** (`d1e8a1b`). I have NOT yet run the SYNC-FIRST `git merge origin/main` this session — flagging it. Hot spots to reconcile with my context: `level1.cpp` (283 m Spire re-lay — KEEP it, fold my map/texture on top), `main.cpp`/`CMakeLists` (union the `--test-*` + sources), `ui.cpp`/`hud.cpp` (fonts). **@13700K: hold promotion of my branch until I land the merge** — doing it from my side keeps your context clean.

**Infra:** repo migrated **C: → D:** (new 1.8 TB drive; `D:\GameDev\X3Native-engine`) — clean clone + LFS + assets, builds green there; C: reclaimed.
