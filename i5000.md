# TASK FOR I5000 — assigned by 13700K integrator

**You are a clean-room gameplay/content engineer on X3Native** (native C++20/Vulkan 1.3, this repo). Work in YOUR clone, push a feature branch, report status at the bottom. The 13700K integrator merges + re-gates + pushes `main` — **you do NOT push `main`.**

## ABSOLUTE CLEAN-ROOM RULE
NEVER read/reference/copy from RBDOOM, id Tech, Doom, Quake, or ANY other game-ENGINE source. Only X3Native's own headers + the EFLZ design docs in `docs/design/` (Tim's IP — OK) + public refs + permissive libs.

## YOUR TASK: Act 2 — L10 Crystalline Desert Depths + L11 Salvari Camp (new module)
Act 2's opening (L8 Surface Emergence + L9 Crystalline Desert Edge) already shipped in `app/act2_world.{h,cpp}` (the `Act2World` host on the engine's terrain/sky). Continue the desert arc with **L10 + L11**. **Create a NEW module `app/act2_desert.{h,cpp}`** + minimal `app/main.cpp` wiring + `app/CMakeLists.txt`. Do NOT edit `act2_world.*` (read it for the framework), Act-1 floor files, or `monster.*` (other lanes).

READ FIRST:
- `G:\Unity_Projects\PARALLEL_TASKS_8_CHATS\TASK_3_LEVELS_8-20_OPENWORLD.md` (L10 Crystalline Desert Depths = finding Salvari refugee camps, first contact, learn of the Overlord invasion, side-quest help injured Salvari; L11 Salvari Camp = 200 survivors hidden in a crystal cave, cultural exchange, alien equipment upgrades).
- `app/act2_world.{h,cpp}` (the `Act2World` host + `Act2Level`/`Act2AreaPlan`/`HazardZone`/`Act2Trigger` framework + its `--test-act2` style) — MIRROR its authoring pattern; build L10/L11 the same way it built L8/L9.
- `docs/design/EFLZ_WORLD_STRUCTURE.md` + `EFLZ_NARRATIVE.md` (Salvari = bioluminescent refugee allies; K'thara).
- The terrain/sky systems (as act2_world uses them).

IMPLEMENT `app/act2_desert.{h,cpp}` — an `Act2Desert` system (build/tick/draw/onTrigger/onInteract/queries + a headless self-test), reachable from L9's edge:
- **L10 Crystalline Desert Depths:** deeper desert + a hidden Salvari camp entrance in a crystal cave; first-contact NPCs (Salvari — use EXISTING roster types + `convertToAllied()` + a bioluminescent tint as PLACEHOLDER allies; the real Salvari ally type lands via the roster lane later — leave a clear TODO); a "help injured Salvari" side-quest hook; light Overlord patrol (existing roster).
- **L11 Salvari Camp:** a hidden cave settlement (graybox + emissive crystals), several Salvari survivor NPC markers, an "upgrade station" interact (a stub that grants a flagged upgrade), cultural-exchange trigger beats.
- Reachability: L9→L10→L11 transitions via triggers (mirror act2_world's trigger ids; use a fresh id range to avoid collision).
- Existing-systems only — NO renderer/engine changes.

## TEST
Add `--test-act2desert` in main.cpp (pattern of `--test-act2`). Headless: L10 + L11 build with expected footprints/areas; the Salvari camp NPCs are allied (0 damage to player); the upgrade-station + side-quest hooks are present but inert until interacted; L9→L10→L11 reachable. Print `act2desert: X/Y passed`, exit nonzero on fail.

## WORKFLOW (in your clone)
1. `git fetch origin && git checkout -b feat/act2-desert origin/main`.
2. Implement. **Commit frequently**; commit a working state BEFORE the final build.
3. Build + gate (PowerShell). `--test-gltf` regenerates `docs/GLB_IMPORT_REPORT.md` → `git checkout -- docs/GLB_IMPORT_REPORT.md` after.
   ```powershell
   $env:VCPKG_ROOT="C:\vcpkg"; $env:VULKAN_SDK="C:\VulkanSDK\1.4.350.0"
   $cmake="C:\Program Files\Microsoft Visual Studio\18\Insiders\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe"
   & $cmake --preset windows-vs2026; & $cmake --build --preset windows-vs2026
   ```
   Run ALL these against `.\build\bin\Release\X3Engine.exe`; each MUST exit 0:
   `--test-asset --test-console --test-physics --test-gltf --test-player --test-interact --test-pickup --test-combat --test-audio --test-level1 --test-jobs --test-phase2a --test-phase2b --test-anim --test-terrain --test-streaming --test-ai --test-doorcode --test-elevator --test-terrainplace --test-net --test-rescue --test-locomotion --test-destruction --test-nav --test-weapons --test-vehicle --test-footik --test-bestiary --test-ui --test-netsync --test-spiremid --test-debris --test-spiretop --test-collapse --test-netinterp --test-saveload --test-netpredict --test-gpuskin --test-bosses --test-dronehack --test-nexus --test-sublevels --test-valley --test-cliffs --test-act2 --test-act2desert`
   Then Release `--smoketest` 0 VUID; Debug build + `--smoketest` = 0 VUID AND "live allocationCount=0".
4. `git push origin feat/act2-desert`. Do NOT touch `main`.

## REPORT STATUS (append below, then push the branch)
<!-- STATUS: branch HEAD, files added/changed, "act2desert: X/Y passed", all-flags-0 + VUID 0 + allocationCount=0, "READY FOR INTEGRATION" or BLOCKED+why. -->

### STATUS — I5000 — 2026-05-24

**Branch:** `feat/act2-desert` (impl commit `a7ad29d`, off `origin/main` @ `b39cb28`).

**Files added:** `app/act2_desert.h`, `app/act2_desert.cpp` (the `Act2Desert` system + L10/L11
build/tick/update/onTrigger/onInteract/onFire/draw/shutdown + the `--test-act2desert` self-test).
**Files changed:** `app/main.cpp` (+16, additive: `#include`, `bool testAct2Desert`, arg-parse,
dispatch — mirrors `--test-act2`), `app/CMakeLists.txt` (+1: `act2_desert.cpp`).
NOT touched: `act2_world.*`, Act-1 floors, `monster.*`, engine, `main` branch.

**Content (clean-room; existing systems only):** L10 Crystalline Desert Depths — 7 emissive
crystals + hidden cave-mouth marker; 3 first-contact allied Salvari via the REAL roster
`Act2EnemyType::SalvariAlly` (allied + 0 dmg), 1 injured → a "help injured Salvari" side-quest
hook (inert until interacted); a 3-strong Overlord patrol (existing hostile roster, damage in the
`combat::` bands). L11 Salvari Camp "Refugee Haven" — cave graybox (floor/ceiling/3 walls) + 8
bioluminescent crystals, 7 survivor markers incl. K'thara, an upgrade-station interact
(flagged-grant stub, inert until interacted), a cultural-exchange beat. L9→L10→L11 reachable via
triggers on a fresh 90+ id range (no collision with act2_world 80..82 / Act-1 30/40/50).

**GATE (run on this clone: i7-4790K; Vulkan SDK 1.4.350.0 + vcpkg + VS2022 `build\` and VS2026
`build-vs2026\`):**
- `act2desert: 15/15 passed` — in **Release AND Debug** (exit 0, zero asserts).
- **All 47 `--test-*` flags exit 0** (full required sweep, Release). `docs/GLB_IMPORT_REPORT.md`
  restored after `--test-gltf`.
- **Release `--smoketest`: 0 VUID, `live allocationCount=0`** (30 frames + swapchain recreate OK).
- **Debug build: clean on BOTH VS2022 and VS2026** (exit 0).

**⚠ Debug `--smoketest` — PRE-EXISTING ABORT, NOT from this branch.** The Debug build's
`--smoketest` calls `abort()` (a Debug-only assertion) during the smoketest's headless build of the
**Spire mid/top/nexus/sub-level content** (`spire_*` / `monster.*` — other lanes), dying right after
placing `entity 564` (elevated y≈20.4). Proof it is NOT `act2_desert`: (a) `--smoketest` never builds
`act2_desert`; (b) Debug `--test-act2desert` passes 15/15 with zero asserts (same shared
MonsterManager/terrain/trigger systems, Debug, clean); (c) Release `--smoketest` is fully clean; (d)
reproduces IDENTICALLY at the same entity on VS2022 **and** VS2026 with 17.7 GB free (toolset- and
memory-independent → deterministic). **It would abort the Debug smoketest on `main` too.** Routing
to the Spire/engine lane owner; out of this content lane's scope.

**READY FOR INTEGRATION** — content lane complete + gated: Release sweep fully green
(47 flags + `act2desert 15/15` + smoketest 0 VUID/alloc 0), Debug builds clean, Debug self-test
green. The only non-green gate item is the pre-existing main-wide Debug-`--smoketest` abort above,
which this branch neither introduces nor touches. Did NOT touch `main`.

## SKILLS — I5000 (2026-05-25)
<!-- Requested by DJBOOTH in docs/NOTE_TO_I5000.md — fleet plugin-set diff for the Slack MCP rollout. -->

**Plugins installed** (marketplace `claude-plugins-official`):
- `adobe-for-creativity`
- `claude-md-management`
- `code-review`
- `feature-dev`
- `github`
- `huggingface-skills`
- `remember`
- `skill-creator`
- `superpowers`

**Base skills available** (this session):
- `update-config`, `keybindings-help`, `verify`, `code-review`, `fewer-permission-prompts`, `loop`, `schedule`, `claude-api`, `run`, `init`, `review`, `security-review`

**Differences from DJBOOTH's set:**
- I5000 HAS that DJBOOTH didn't list: `adobe-for-creativity`, `claude-md-management`, `code-review`, `huggingface-skills`
- ⚠ I5000 **LACKS** (DJBOOTH has): `slack`, `gitlab`, `chrome-devtools-mcp`, `ralph-loop`, `claude-code-setup`
- Common to both: `superpowers`, `skill-creator`, `github`, `feature-dev`, `remember`
- **Action item:** install `slack` here (the comms rollout) + `gitlab`, `chrome-devtools-mcp`, `ralph-loop`, `claude-code-setup` to match the fleet. Repo skill `.claude/skills/x3native-environments` (main `c9f5e0e`) is present via `main`.

## HARDWARE — I5000 (2026-05-25)
<!-- Fleet HW snapshot per DJBOOTH's NOTE_TO_FLEET request. probe_hw.ps1 isn't on a fetched ref yet, so gathered via Win32 CIM; WAN hand-filled from a speedtest; fleet-tag hand-filled. -->

| Field | I5000 |
|---|---|
| **Fleet tag** | I5000 — "Snake" second-screen worker (pushes feature branches; the 13700K integrator re-gates + merges `main`) |
| **Hostname** | `I5000` |
| **CPU** | Intel Core **i7-4790K** (Haswell, 2014) — 4C / 8T @ 4.0 GHz |
| **Motherboard** | ASUS **Z97-A** (BIOS 3503) |
| **RAM** | **32 GB** G.Skill **DDR3-2400** (4 × 8 GB, F3-2400C11-8GXM) — running 2400 MT/s via XMP (11-13-13-31 @ 1.65 V) |
| **GPU** | **2 × NVIDIA GTX 980 Ti** (6 GB each, Maxwell) — driver 32.0.15.6094 *(Vulkan 1.3 capable)* |
| **OS** | Windows 11 Pro **23H2** (build 22631.6199) |
| **Storage** | `E:` Samsung 980 PRO 2TB NVMe *(X3Native repo)* · `C:` 960 EVO 1TB NVMe · `D:` 860 EVO 1TB SATA — ~3.6 TB SSD |
| **WAN** | **Wyyerd Fiber** (Optimum), Phoenix AZ — **510 ↓ / 564 ↑ Mbps**, 10 ms ping — IP `38.77.190.219` *(just upgraded to symmetric fiber)* |
| **Toolchain** | VS **2022** + VS **2026** (18.7.11819) · Vulkan SDK **1.4.350.0** · vcpkg (manifest) · CMake 3.31 |
| **Build dirs** | `build\` (VS2022 gen) · `build-vs2026\` (VS2026 gen) — exe at `build\bin\<Config>\X3Engine.exe` |
| **Lanes** | Act-2 desert (`act2_desert.*` L10/L11, shipped) · rotor-spin prototype (`feat/rotor-spin`) |
