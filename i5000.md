# TASK FOR I5000 — assigned by 13700K integrator

**You are a clean-room gameplay/content engineer on X3Native** (native C++20/Vulkan 1.3, this repo). Work in YOUR clone, push a feature branch, report status at the bottom. The 13700K integrator merges + re-gates + pushes `main` — **you do NOT push `main`.**

## ABSOLUTE CLEAN-ROOM RULE
NEVER read/reference/copy from RBDOOM, id Tech, Doom, Quake, or ANY other game-ENGINE source. Only X3Native's own headers + the EFLZ design docs in `docs/design/` (Tim's IP — OK) + public refs + permissive libs.

## YOUR TASK: Act 2 — L10 Crystalline Desert Depths + L11 Salvari Camp (new module)
Act 2's opening (L8 Surface Emergence + L9 Crystalline Desert Edge) already shipped in `app/act2_world.{h,cpp}` (the `Act2World` host on the engine's terrain/sky). Continue the desert arc with **L10 + L11**. **Create a NEW module `app/act2_desert.{h,cpp}`** + minimal `app/main.cpp` wiring + `app/CMakeLists.txt`. Do NOT edit `act2_world.*` (read it for the framework), Act-1 floor files, or `monster.*` (other lanes).

READ FIRST:
- `G:\Unity_Projects\PARALLEL_TASKS_8_CHATS\TASK_3_LEVELS_8-20_OPENWORLD.md` (L10 Crystalline Desert Depths = finding Salvari refugee camps, first contact, learn of the Overlord invasion, side-quest help injured Salvari; L11 Salvari Camp = 200 survivors hidden in a crystal cave, cultural exchange, alien equipment upgrades).
- `app/act2_world.{h,cpp}` (the `Act2World` host + `Act2Level`/`Act2AreaPlan`/`HazardZone`/`Act2Trigger` framework + its `--test-act2` style) — MIRROR its authoring pattern; build L10/L11 the same way it built L8/L9.
- `docs/design/EFLZ_WORLD_STRUCTURE.md` + `EFLZ_NARRATIVE.md` (Salvari = bioluminescent refugee allies; K'thara).
- The terrain/sky systems (as act2_world uses them).

IMPLEMENT `app/act2_desert.{h,cpp}` — an `Act2Desert` system (build/tick/draw/onTrigger/onInteract/queries + a headless self-test), reachable from L9's edge:
- **L10 Crystalline Desert Depths:** deeper desert + a hidden Salvari camp entrance in a crystal cave; first-contact NPCs (Salvari — use EXISTING roster types + `convertToAllied()` + a bioluminescent tint as PLACEHOLDER allies; the real Salvari ally type lands via the roster lane later — leave a clear TODO); a "help injured Salvari" side-quest hook; light Overlord patrol (existing roster).
- **L11 Salvari Camp:** a hidden cave settlement (graybox + emissive crystals), several Salvari survivor NPC markers, an "upgrade station" interact (a stub that grants a flagged upgrade), cultural-exchange trigger beats.
- Reachability: L9→L10→L11 transitions via triggers (mirror act2_world's trigger ids; use a fresh id range to avoid collision).
- Existing-systems only — NO renderer/engine changes.

## TEST
Add `--test-act2desert` in main.cpp (pattern of `--test-act2`). Headless: L10 + L11 build with expected footprints/areas; the Salvari camp NPCs are allied (0 damage to player); the upgrade-station + side-quest hooks are present but inert until interacted; L9→L10→L11 reachable. Print `act2desert: X/Y passed`, exit nonzero on fail.

## WORKFLOW (in your clone)
1. `git fetch origin && git checkout -b feat/act2-desert origin/main`.
2. Implement. **Commit frequently**; commit a working state BEFORE the final build.
3. Build + gate (PowerShell). `--test-gltf` regenerates `docs/GLB_IMPORT_REPORT.md` → `git checkout -- docs/GLB_IMPORT_REPORT.md` after.
   ```powershell
   $env:VCPKG_ROOT="C:\vcpkg"; $env:VULKAN_SDK="C:\VulkanSDK\1.4.350.0"
   $cmake="C:\Program Files\Microsoft Visual Studio\18\Insiders\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe"
   & $cmake --preset windows-vs2026; & $cmake --build --preset windows-vs2026
   ```
   Run ALL these against `.\build\bin\Release\X3Engine.exe`; each MUST exit 0:
   `--test-asset --test-console --test-physics --test-gltf --test-player --test-interact --test-pickup --test-combat --test-audio --test-level1 --test-jobs --test-phase2a --test-phase2b --test-anim --test-terrain --test-streaming --test-ai --test-doorcode --test-elevator --test-terrainplace --test-net --test-rescue --test-locomotion --test-destruction --test-nav --test-weapons --test-vehicle --test-footik --test-bestiary --test-ui --test-netsync --test-spiremid --test-debris --test-spiretop --test-collapse --test-netinterp --test-saveload --test-netpredict --test-gpuskin --test-bosses --test-dronehack --test-nexus --test-sublevels --test-valley --test-cliffs --test-act2 --test-act2desert`
   Then Release `--smoketest` 0 VUID; Debug build + `--smoketest` = 0 VUID AND "live allocationCount=0".
4. `git push origin feat/act2-desert`. Do NOT touch `main`.

## REPORT STATUS (append below, then push the branch)
<!-- STATUS: branch HEAD, files added/changed, "act2desert: X/Y passed", all-flags-0 + VUID 0 + allocationCount=0, "READY FOR INTEGRATION" or BLOCKED+why. -->
